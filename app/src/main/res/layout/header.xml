<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="180dp"
    android:gravity="bottom"
    android:background="@drawable/border_theme"
    android:orientation="vertical">


    <ImageView
        android:id="@+id/proimage"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginTop="60dp"
        android:scaleY="0.9"
        android:scaleX="0.9"
        app:layout_constraintEnd_toEndOf="@+id/mailidtext"
        app:layout_constraintStart_toStartOf="@+id/mailidtext"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/mailidtext"
        android:layout_width="0dp"
        android:layout_height="30dp"
        android:layout_marginStart="30dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="20dp"
        android:fontFamily="@font/ubuntu"
        android:textColor="#FFFFFF"
        android:textSize="15dp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/proimage" />


</androidx.constraintlayout.widget.ConstraintLayout>