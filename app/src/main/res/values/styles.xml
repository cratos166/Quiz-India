<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/layer_1</item>
        <item name="colorPrimaryDark">@color/layer_1</item>
        <item name="colorAccent">@color/layer_1</item>
    </style>

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <style name="ThemeOverlay.titleColor" parent="">
        <item name="android:textColorSecondary">@color/mainmenubuttonback</item>
    </style>

</resources>